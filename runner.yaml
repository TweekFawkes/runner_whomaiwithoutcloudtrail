inputs:
  - name: accesskeyid
    prompt: AWS Access Key ID
    type: text
  - name: secretaccesskey
    prompt: AWS Secret Access Key
    type: text

launch:
  program: app.py
  args:
    - '--accesskeyid'
    - '${accesskeyid}'
    - '--secretaccesskey'
    - '${secretaccesskey}'
