inputs:
  - name: accesskeyid
    prompt: AWS Access Key ID
    type: text
  - name: AWS Secret Access Key
    prompt: ip_address
    type: text

launch:
  program: app.py
  args:
    - '--accesskeyid'
    - '${accesskeyid}'
    - '--secretaccesskey'
    - '${secretaccesskey}'
